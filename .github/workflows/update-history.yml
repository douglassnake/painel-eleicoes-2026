name: Verificar histórico eleitoral

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Status da integração histórica
        run: |
          echo "Os arquivos históricos do TSE também retornam HTTP 403 para runners hospedados do GitHub Actions."
          echo "Execute atualizar_dados_eleitorais.bat em uma máquina com acesso normal ao TSE."
          echo "O script local gera official-data.json e election-history.json e pode publicar ambos no repositório."
