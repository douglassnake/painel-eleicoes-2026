name: Verificar sincronização TSE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Status da integração
        run: |
          echo "Os endpoints do TSE estão retornando HTTP 403 para os IPs dos runners hospedados do GitHub Actions."
          echo "A atualização oficial deve ser executada localmente por atualizar_dados_eleitorais.bat."
          echo "O GitHub Pages continua sendo publicado normalmente após o push dos JSON gerados."
